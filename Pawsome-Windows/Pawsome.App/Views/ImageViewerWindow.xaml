<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Pawsome.App.Views.ImageViewerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="Black">
        <ScrollViewer ZoomMode="Enabled" MinZoomFactor="1" MaxZoomFactor="6"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                      HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled">
            <Image x:Name="FullImage" Stretch="Uniform" />
        </ScrollViewer>

        <Button Click="Close_Click" HorizontalAlignment="Right" VerticalAlignment="Top"
                Margin="12" Width="40" Height="40" CornerRadius="20" Padding="0"
                Background="#99000000">
            <FontIcon Glyph="&#xE711;" Foreground="White" />
        </Button>
    </Grid>
</Window>
