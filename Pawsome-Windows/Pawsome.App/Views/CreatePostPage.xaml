<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Pawsome.App.Views.CreatePostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ScrollViewer>
            <StackPanel MaxWidth="560" Padding="20" Spacing="18" HorizontalAlignment="Stretch">

                <!-- Header -->
                <Grid>
                    <Button Background="Transparent" BorderThickness="0" HorizontalAlignment="Left"
                            Click="Back_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon Glyph="&#xE72B;" FontSize="14" />
                            <TextBlock Text="Back" />
                        </StackPanel>
                    </Button>
                    <TextBlock Text="New Post" FontWeight="SemiBold" FontSize="16"
                               HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Grid>

                <!-- Image preview / picker -->
                <Border Style="{StaticResource CardSurfaceStyle}" Height="240">
                    <Grid>
                        <Image Source="{x:Bind ViewModel.Preview, Mode=OneWay}" Stretch="Uniform"
                               Visibility="{x:Bind ViewModel.HasImage, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8"
                                    Visibility="{x:Bind ViewModel.HasImage, Mode=OneWay, Converter={StaticResource InvBoolToVis}}">
                            <FontIcon Glyph="&#xEB9F;" FontSize="40"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="No image selected"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                        </StackPanel>
                    </Grid>
                </Border>

                <Button HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        Click="ChooseImage_Click">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xEB9F;" FontSize="16" />
                        <TextBlock Text="Choose Image" />
                    </StackPanel>
                </Button>

                <!-- Fields -->
                <TextBox Header="Cat Name" PlaceholderText="e.g. Mittens"
                         Text="{x:Bind ViewModel.CatName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Header="Age (years)" PlaceholderText="e.g. 3"
                         InputScope="Number"
                         Text="{x:Bind ViewModel.Age, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Header="Description" PlaceholderText="Where was the cat seen? Any details?"
                         TextWrapping="Wrap" AcceptsReturn="True" Height="110"
                         Text="{x:Bind ViewModel.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <InfoBar IsOpen="True" Severity="Error"
                         Message="{x:Bind ViewModel.Error, Mode=OneWay}" IsClosable="False"
                         Visibility="{x:Bind ViewModel.Error, Mode=OneWay, Converter={StaticResource StringToVis}}" />

                <!-- Submit -->
                <Button Style="{StaticResource BrandButtonStyle}"
                        Command="{x:Bind ViewModel.SubmitCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <ProgressRing IsActive="{x:Bind ViewModel.IsPosting, Mode=OneWay}"
                                      Width="18" Height="18" Foreground="White"
                                      Visibility="{x:Bind ViewModel.IsPosting, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
                        <TextBlock Text="Post 🐾" />
                    </StackPanel>
                </Button>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
