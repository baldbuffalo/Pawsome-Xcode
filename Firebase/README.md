# Firebase

Firebase configuration and backend files for Pawsome.

- `iOS/` — iOS Firebase app configuration files
- `Android/` — Android Firebase app configuration files
- `firestore.rules` — Firestore security rules
- `firestore.indexes.json` — Firestore indexes
- `functions/` — Firebase Cloud Functions
